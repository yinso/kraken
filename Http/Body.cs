using System;
using System.Net;

namespace Http
{
    // dealing with Body is hard.
    // we have a few things here... once we decide to make use of System.Net.
    // application/x-www-url-form-encoded
    // multipart/form-data (MIME)
    public class Body
    {
        public Body()
        {
            // 
        }
    }
}

